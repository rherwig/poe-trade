export default class MessageService {
    parse(raw: string) {
        // parse raw to get message part
        // infer type based on message part
        // ?convert message to matching type?
        // return message in correct type
    }
}
